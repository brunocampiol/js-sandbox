// Template literal for string concatenation and interpolation
let part1 = "Javascript Dynamically";
let part2 = "Generated Content!";
let message = `${part1}, ${part2}`;  // Uses backticks to embed variables directly in the string

// Select the <p> element by its ID and set its text content
document.getElementById('targetMessage').innerText = message;